basic test server. reusable for other web projects

# initial build
1. `bash build-server.sh`

# run
run `server.exe`. don't need to rebuild anymore (unless change code, but that usually won't happen)